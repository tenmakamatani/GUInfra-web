import { css } from "@emotion/core";

export default {
  wrapper: css({
    width: "100%",
    marginBottom: 20
  }),
  select: css({
    width: "100%",
    height: 30,
    marginTop: 5,
    marginBottom: 5
  })
};
