import { css } from "@emotion/core";

export default {
  wrapper: css({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "100%",
  }),
  wrapperInner: css({
    textAlign: "center",
  }),
  img: css({
    width: "80%",
    height: "auto"
  }),
}
