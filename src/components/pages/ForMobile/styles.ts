import { css } from "@emotion/core";

export default {
  wrapper: css({
    textAlign: "center",
    width: "100%",
    height: "100%",
  }),
  img: css({
    width: "80%",
    height: "auto"
  }),
}
